@model IEnumerable<VideoDataViewModel>

<div class="top-grids">
    <div class="recommended-info">
        <h2>Recent Videos</h2>
    </div>
    @foreach (var video in Model)
    {
        @Html.Partial("_RecentVideo", video);
    }
    <div class="clearfix"> </div>
</div>
